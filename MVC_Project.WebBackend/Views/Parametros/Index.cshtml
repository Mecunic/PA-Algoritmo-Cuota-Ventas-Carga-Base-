@model  MVC_Project.WebBackend.Models.ParametrosViewModel

@{
    ViewBag.Title = "Administración de Parámetros";
}
@section styles {
    @Styles.Render("~/plugins/dataTablesStyles")
}

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-12">
            <div class="ibox float-e-margins">
                @using (Html.BeginForm("GetAllByFilter", "Parametros", FormMethod.Post, new { @id = "SearchForm" }))
                {
                    <div class="ibox-title">
                        <div class="row">
                            <div class="col-sm-6 col-xs-12">
                                <div class="form-group row">
                                    <label class="col-lg-2 col-sm-3">Parámetros</label>
                                    <div class="col-lg-10 col-sm-9">
                                        @Html.TextBoxFor(m => m.Filtro, new { @Class = "form-control form-control-sm txtFiltro", maxlength = "100", filtro = "true", filtro_order = "1" })
                                    </div>
                                </div>
                            </div>
                            <div class="col-sm-6 col-xs-12">
                                <button type="button" id="btnSearchForm" class="btn btn-sm btn-primary btn-filter">Buscar</button>
                                <button type="button" class="btn btn-create btn-primary float-right"><span class="fas fa-plus"></span> Crear</button>
                                <div class="clearfix"></div>
                            </div>

                        </div>
                    </div>
                    <div class="ibox-content clearfix">


                        <div class="table-responsive">
                            <table id="table" class="table table-striped table-bordered table-hover"></table>
                        </div>

                    </div>
                }
            </div>
        </div>

    </div>
</div>


@section scripts{

    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/custom/Utils")
    <script src="~/Scripts/views/parametros/Index.js"></script>

    <script>
        var tableCtrl;
        $(function () {
            tableCtrl = new ParametroIndexControlador('table', '@Url.Action("GetAllByFilter")');
            tableCtrl.init();
        });
        const utils = new Utils();
        $(window).keydown(function (event) {
            if (event.keyCode == 13) {
                event.preventDefault();
                return false;
            }
        });


        $("#btnClearForm").click(function () {
            $("#SearchForm").each(function () {
                this.reset();
            });
            $('#table').DataTable().draw();
        });

        $(".btn-filter").click(function () {
            $('#table').DataTable().draw();
        });


        $(".btn-create").click(function () {
            location.href = '@Url.Action("Create")';
        });


    </script>

}
