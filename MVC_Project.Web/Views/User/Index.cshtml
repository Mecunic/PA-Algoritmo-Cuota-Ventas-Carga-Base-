@model  MVC_Project.Web.Models.UserViewModel

@{
    ViewBag.Title = "Administración de Usuarios";
}
@section styles {
    @Styles.Render("~/Content/template/plugins/dataTables/dataTablesStyles")
}

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <span class="fa-stack">
                <i class="fa fa-lock fa-stack-2x"></i>
                <i class="fa fa-edit fa-stack-1x fa-inverse"></i>
            </span>

            <span class="fa-stack fa-2x">
                <i class="fas fa-camera fa-stack-1x"></i>
                <i class="fas fa-ban fa-stack-2x" style="color:Tomato"></i>
            </span>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                @using (Html.BeginForm("GetAllByFilter", "User", FormMethod.Post, new { @id = "SearchForm" }))
                {
                    <div class="ibox-title">
                        <h5>Búsqueda</h5>
                    </div>
                    <div class="ibox-content clearfix">
                        <div class="row">
                            <div class="col-xs-4">
                                <div class="form-group">
                                    <label>Nombre o correo electrónico</label>
                                    @Html.TextBoxFor(m => m.Name, new { @Class = "form-control form-control-sm txtFiltro", maxlength = "100", filtro = "true", filtro_order = "1" })
                                </div>
                            </div>

                            <div class="col-xs-4">
                                <div class="form-group">
                                    <label>Estado</label>
                                    @Html.DropDownListFor(x => x.Status, Model.Statuses, new { @class = "selectpicker form-control", data_live_search = "true", data_style = "btn-default", style = "position: relative;", filtro = "true", filtro_order = "2" })
                                </div>
                            </div>

                            <div class="col-xs-12">
                                <button type="button" class="btn btn-sm btn-primary btn-filter-user pull-right"><i class="fas fa-search"></i> Buscar</button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>

        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>Resultado</h5>
                    <div class="ibox-tools">
                        <a href="@Url.Action("Create","User")" class="btn btn-primary"><span class="fas fa-plus-square"></span> Crear Nuevo</a>
                        <a href="@Url.Action("Import","User")" class="btn btn-primary"><span class="fas fa-file-import"></span> Importar</a>

                    </div>
                </div>
                <div class="ibox-content">
                    <table id="table" class="table table-striped table-bordered"></table>
                </div>
            </div>
        </div>
    </div>
</div>

<div id="modal-edit-password" class="modal fade hidden-print" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-dialog-centered" role="dialog">
        <div class="modal-content"></div>
    </div>
</div>
@section scripts{
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/fullCalendar")
    <script src="~/Scripts/template/Utils.js"></script>
    <script src="~/Scripts/views/User/Index.js"></script>

    <script>
        $(function () {
            var usuarios = new UserIndexControlador('table', '@Url.Action("GetAllByFilter")',
                '@Url.Action("ModalEditAction")', '@Url.Action("ModalDeleteAction")', 'modal-edit-password');
            usuarios.init();
        });

        $(".btn-filter-user").click(function () {
            $('#table').DataTable().draw();
        });
    </script>

}